window.IGCSE={password:'igcse123',loadUser:(name,klass)=>{const key=`igcse_user_${name}_${klass}`.replace(/\s+/g,'_').toLowerCase();const saved=localStorage.getItem(key);if(saved)return JSON.parse(saved);const user={key,name,klass,completed:[],scores:{}};localStorage.setItem(key,JSON.stringify(user));return user;},saveUser:(user)=>localStorage.setItem(user.key,JSON.stringify(user))};